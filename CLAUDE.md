## First Step — Always

**Before making any changes, check the Obsidian vault (MCP) for context:**
- Search `atomicsync` in Obsidian to find architecture decisions, vision, progress, and design research
- Key vault areas: `Areas/AtomicSync-March/` — companion app vision, design research, progress tracker
- This ensures you have the latest project context before acting

## Related Repositories

All repos are at `~/Desktop/CentralProject/`:
- `atomicsync-web` — This repo
- `atomicsync-ios` — iOS app (Swift, SwiftUI)
- `atomicsync-avatar-engine` — Avatar generation pipeline (Python)
- `atomicsync-infra` — AWS infrastructure, CloudFormation, Lambda

@AGENTS.md
